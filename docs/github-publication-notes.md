# GitHub Publication Notes

## Suggested GitHub Description

Full-stack AI workflow automation dashboard built with Next.js, React, TypeScript and .NET 8.

## Suggested GitHub Topics

- `nextjs`
- `react`
- `typescript`
- `dotnet`
- `csharp`
- `sqlite`
- `ai`
- `workflow-automation`
- `internal-tools`
- `full-stack`
- `portfolio`

## Suggested Visibility Note

The repository can be made public after the validation checklist passes and screenshots/docs are confirmed.

Before switching visibility to public, confirm:

- README screenshot paths render correctly on GitHub.
- No `.env` files are tracked.
- No local SQLite database files are tracked.
- No `node_modules`, `.next`, `bin` or `obj` folders are tracked.
- Public-facing copy does not claim production usage, real clients or invented metrics.
- The AI integration is described honestly as mock-based and abstraction-ready.
